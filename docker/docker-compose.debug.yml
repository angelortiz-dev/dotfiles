services:
  api:
    build: .
    command: >
      python -m debugpy --listen 0.0.0.0:5678
      -m uvicorn app.main:app --reload --host 0.0.0.0
    ports:
      - "8000:8000"
      - "5678:5678"
    env_file:
      - .env
